// Same card as the Open Graph image — X/Twitter requires its own route to
// emit twitter:image alongside the summary_large_image card.
export { default, alt, size, contentType } from "./opengraph-image";
